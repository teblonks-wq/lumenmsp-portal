<%- include('header', { title: 'OneBoard', user, active: 'oneboard', company: typeof company !== 'undefined' ? company : '' }) %>

<div class="page-header" style="margin-bottom:14px;">
  <h1 style="margin:0;font-size:26px;letter-spacing:-.5px;">OneBoard</h1>
  <p class="muted" style="margin:4px 0 0;">Dashboard for the whole company — <strong><%= insName || company %></strong></p>
</div>

<%- include('../partials/oneboard-board', { obAction: '/my/oneboard', obExportBase: '/my/oneboard', obShowSave: true, obExtraHidden: '', obExtraQs: '' }) %>

<%- include('footer') %>
